import { AppTrackScrollDirective } from './app-track-scroll.directive';

describe('AppTrackScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTrackScrollDirective();
    expect(directive).toBeTruthy();
  });
});
